# RAG_Agent runtime dependencies (Linux/macOS friendly)
# Heavy ML deps (faiss, sentence-transformers, torch) are required for
# real retrieval. The pipeline degrades gracefully when LLM (Ollama)
# is unreachable so unit tests can run without those services.

# --- Core RAG ---
faiss-cpu>=1.8
sentence-transformers>=2.7
numpy>=1.26
requests>=2.31

# --- Text cleaning / chunking ---
wordninja>=2.0
nltk>=3.8
tabulate>=0.9

# --- Output / UX ---
Pillow>=10.0       # used by docs/render_demos.py to make terminal-style screenshots

# --- Tests ---
pytest>=8.0
faiss-cpu
nltk
Pillow
pytest
requests
sentence-transformers
tabulate
wordninja
faiss-cpu
sentence-transformers
