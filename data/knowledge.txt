If missing values are less than 30 percent, imputation is generally preferred.

For numeric columns, mean or median imputation can be used depending on distribution.

Median is more robust for skewed data.

Columns with very high missing values (above 70 percent) should be considered for removal.

Categorical missing values are usually filled using mode.

High cardinality categorical variables should not be one-hot encoded.